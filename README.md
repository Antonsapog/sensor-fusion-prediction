# Sensor-Fusion-Forecast

Алгоритм восстановления пропущенных значений сигнала датчика с использованием показаний всех остальных датчиков на объекте через многомерную фильтрацию.

## 📊 Описание

Проект реализует метод восстановления пропущенных/искаженных значений в 2D-сигналах (например, данных с сетки датчиков) с использованием:
- **Синтеза оптимальных 2D-фильтров** (ФНЧ/ФВЧ) методом линейного программирования
- **Разделения сигнала** на низкочастотную и высокочастотную компоненты
- **Последовательного восстановления** каждой компоненты с учетом пространственной корреляции
- **Адаптивной обработки** пропущенных данных через нормализацию маской

## ✨ Основные возможности

- **Синтез 2D ФНЧ** с заданными параметрами (отсечка, переходная полоса)
- **Разделение сигнала** на НЧ и ВЧ компоненты с коррекцией пропусков
- **Восстановление дырок**:
  - НЧ компонента: через локальную аппроксимацию
  - ВЧ компонента: через дополнительный ФВЧ-фильтр
- **Автоматическая оценка качества**: MSE, PSNR, MAE, относительная ошибка
- **Визуализация** результатов восстановления

## 📁 Структура проекта
├── sensor_fusion.py # Основной исполняемый файл с полным алгоритмом
├── requirements.txt # Зависимости проекта
└── README.md # Документация

### Ключевые функции:

- `design_lp_filter_2d()` – синтез 2D ФНЧ методом линейного программирования
- `recover_component_local()` – восстановление компоненты в точках пропусков
- `low_high_separation_normalized()` – разделение сигнала с коррекцией пропусков
- `naive_fill()` – грубое начальное заполнение дырок (опционально)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Использование в своем проектк

```bash
import numpy as np
from sensor_fusion import design_lp_filter_2d, recover_component_local

# Синтез фильтра
h_filter, delta = design_lp_filter_2d(
    N=21,
    cutoff=0.14,
    transition=0.05,
    G=81
)

# Восстановление компоненты
restored = recover_component_local(
    data_band=data_missing,
    h=h_filter,
    mask_missing=missing_mask
)
```

### Метрики качества
Для оценки используются:

- MSE (Mean Squared Error) – среднеквадратичная ошибка

- PSNR (Peak Signal-to-Noise Ratio) – отношение пикового сигнала к шуму

- MAE (Mean Absolute Error) – средняя абсолютная ошибка в дырках

- Относительная ошибка – в процентах от истинных значений
